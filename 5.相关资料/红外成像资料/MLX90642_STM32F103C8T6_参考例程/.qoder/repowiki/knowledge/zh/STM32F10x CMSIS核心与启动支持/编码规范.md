- 使用`__IO`、`__I`、`__O`宏结合`volatile`限定符定义外设寄存器访问权限，确保编译器不进行优化重排。
- 采用条件编译宏（如`STM32F10X_MD`、`USE_STDPERIPH_DRIVER`）在同一头文件中适配不同芯片密度和外设驱动模式，实现单一源文件的多配置支持。
- 中断向量表在汇编启动文件中以`DCD`指令顺序定义，并与`IRQn_Type`枚举保持严格索引对应，确保硬件中断能正确跳转至处理函数。